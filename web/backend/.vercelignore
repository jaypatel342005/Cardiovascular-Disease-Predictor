# Strict Whitelist Mode

# 1. Ignore EVERYTHING first
*

# 2. Unignore the items we MUST have
!api/
!requirements.txt
!.gitignore

# 3. Explicitly ignore the heavy folders (redundant but safe)
!api/venv/
!api/__pycache__/
